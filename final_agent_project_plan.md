# LLM/Agent 应用开发实习最终学习计划

## 目标

围绕简历核心项目“面向课程资料检索的本地 RAG Agent 系统”，补齐到面试可讲、代码可演示、追问能回答的水平。最终目标不是继续堆功能，而是让项目具备清晰的工程链路：

**文档入库 -> 工具调用 -> LangGraph 状态编排 -> 混合检索 -> 上下文压缩 -> 引用溯源 -> 拒答自检 -> 评测与 benchmark**

## 阶段一：把项目主链路讲透

需要能脱稿讲清楚：

- 这个系统给谁用：用于个人课程资料、技术笔记、项目文档的本地知识库问答。
- 为什么需要 Agent：用户问题不只是问答，还可能触发检索、入库、更新、统计、拒答等不同动作。
- 为什么 RAG 是工具而不是全部：RAG 负责提供可靠资料，Agent 负责任务判断、工具调度和流程控制。
- 一次完整请求链路：用户问题 -> 意图识别 -> 选择工具 -> 检索召回 -> 上下文压缩 -> LLM 生成 -> 引用溯源 -> 自检拒答。

验收标准：

- 能用 2 分钟介绍项目背景和整体架构。
- 能画出项目流程图。
- 能解释 FastAPI、CLI、LangGraph、Function Calling、Chroma、BGE-M3、BM25 各自位置。

## 阶段二：补强 LangGraph 与 Function Calling

重点不是学框架 API，而是把项目里的图设计清楚。

需要完成：

- 定义 Agent State：`query`、`intent`、`chunks`、`tool_trace`、`retry`、`self_check`、`answer`。
- 设计节点：
  - `intent_node`：判断问题类型。
  - `tool_router_node`：决定是否检索、入库、统计或拒答。
  - `retrieve_node`：调用 RAG 检索工具。
  - `compress_node`：压缩上下文。
  - `generate_node`：生成答案。
  - `self_check_node`：判断答案是否有依据。
- 设计边：
  - 正常链路：意图识别 -> 检索 -> 压缩 -> 生成 -> 自检 -> 输出。
  - 失败链路：低相关度 -> 查询改写/再检索。
  - 终止条件：超过最大重试次数或无相关资料。

面试必须能回答：

- LangGraph 里有哪些节点？
- 状态字段为什么这样设计？
- 什么情况下会循环？
- 如何避免无限循环？
- Function Calling 工具有哪些？参数 schema 怎么约束？

## 阶段三：补强 RAG 检索工程细节

这是项目最容易被追问的部分。

需要掌握：

- 文档加载：PDF、Markdown、TXT 如何读入，异常如何处理。
- Chunk 切分：chunk size、overlap、递归切分、语义切分各自作用。
- Embedding：BGE-M3 本地模型做什么，为什么不同模型维度不能混用。
- Chroma：collection 是什么，存了哪些内容，如何增量更新和删除。
- BM25：关键词检索如何补足向量检索的不足。
- 混合检索：向量分数和 BM25 分数如何归一化、加权和排序。
- 阈值过滤：相关性分数太高/太低分别有什么问题。

需要补一个小实验：

- 固定 10-20 个测试问题。
- 分别跑纯向量、纯 BM25、混合检索。
- 记录 Recall@3、Precision@3、平均检索耗时。
- 指标口径要诚实写成“小规模人工标注初步评测”。

## 阶段四：补强上下文压缩与防幻觉

这部分决定项目是否像真正的大模型应用。

需要掌握：

- 上下文是什么：传给 LLM 的参考资料，不是模型自己的知识。
- 为什么要压缩：token 有限制，资料太多会引入噪声。
- 当前压缩策略：
  - 按检索最终分数排序。
  - 去重相同 chunk。
  - 截断到 token/字符预算。
  - 保留文档名、chunk 位置、片段摘要。
- 防幻觉机制：
  - 检索分数低时不生成。
  - Prompt 强制仅依据资料回答。
  - 回答必须带引用。
  - 自检阶段判断答案是否被资料支持。

面试必须能回答：

- 无证据拒答靠 prompt 还是靠代码？
- 自检是规则还是再次调用模型？
- 如果检索错了，LLM 会不会仍然编？
- 引用来源如何生成？

## 阶段五：补强 FastAPI 与工程交付

项目要能演示，不只是命令行跑通。

建议补齐接口：

- `POST /documents/upload`：上传文档。
- `POST /documents/import-dir`：批量导入目录。
- `DELETE /documents/{doc_id}`：删除文档。
- `GET /stats`：查看文档和 chunk 数量。
- `POST /ask`：问答接口，返回 answer、citations、self_check、retrieval_scores。

需要能讲清楚：

- 请求体和响应体如何设计。
- Pydantic 做什么。
- 异常如何返回。
- Swagger 如何测试接口。
- CLI 和 API 为什么都保留。

## 阶段六：补强日志、错误案例和排查经验

面试官喜欢问真实问题，所以要准备失败案例。

至少准备 4 个案例：

- OpenAI API key 鉴权失败：如何定位 `.env` 和 base_url。
- Chroma 维度冲突：为什么 384 和 1024 不能混用，如何重建 collection。
- 本地 BGE-M3 下载慢：如何处理模型缓存和离线依赖。
- 检索无关内容：如何调整 chunk、top_k、BM25 权重和阈值。

每个案例都按这个结构准备：

**现象 -> 原因 -> 排查过程 -> 修复方案 -> 后续避免方式**

## 阶段七：准备面试表达

必须准备以下回答：

1. 你这个项目解决什么问题？
2. 为什么要用 Agent，而不是普通 RAG？
3. LangGraph 里有哪些节点和状态？
4. Function Calling 暴露了哪些工具？
5. BGE-M3、Chroma、BM25 分别做什么？
6. chunk size 和 overlap 怎么选？
7. 混合检索分数怎么合并？
8. 上下文压缩怎么做？
9. 无证据拒答怎么实现？
10. 评测指标怎么设计？样本量是多少？
11. 你遇到过哪些 bug？
12. 如果继续优化，你会做什么？

